"""A/B experiments over the tehai delegation layer.

org_vs_dataflow: compares the human-org hierarchy (A = MetaOrchestrator) against
a verification-centric flat dataflow (B = FlatDataflowOrchestrator) on the same
request suite, measuring reliability, cost, and human-intervention rate.
"""
